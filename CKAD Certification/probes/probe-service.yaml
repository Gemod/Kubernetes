apiVersion: v1
kind: Service
metadata:
  labels:
    app: probe-master
  name: probe-master
spec:
  ports:
  - port: 8080
    protocol: TCP
    targetPort: 8080
  selector:
    app: probe-master
  type: ClusterIP
status:
  loadBalancer: {}
